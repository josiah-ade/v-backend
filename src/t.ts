// docker compose -f docker-compose.local.yml up
// docker compose -f docker-compose.local.yml down
// docker compose -f docker-compose.local.yml down -v --remove-orphans
// docker compose -f docker-compose.local.yml build --build-arg CUSTOM_REGISTRY=https://registry.npmmirror.com
// docker compose -f docker-compose.local.yml build --no-cache --build-arg CUSTOM_REGISTRY=https://registry.npmmirror.com


// docker compose build --build-arg CUSTOM_REGISTRY=https://registry.npmmirror.com